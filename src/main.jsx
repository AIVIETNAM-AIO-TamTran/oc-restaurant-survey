import React from "react";
import ReactDOM from "react-dom/client";
import SurveyChecklist from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SurveyChecklist />
  </React.StrictMode>
);
